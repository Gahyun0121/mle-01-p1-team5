# 통계 대시보드 EDA 분석

## 1. 분석 목적

수집한 해외여행 안전정보를 사용자가 직관적으로 확인할 수 있도록 통계 대시보드를 구성하였다.

대시보드는 다음 네 가지 관점에서 데이터를 탐색할 수 있도록 설계하였다.

* 국가별 여행경보 현황
* 전체 국가와 서비스 대상 국가의 경보단계 분포
* 연도별 안전정보 등록 상위 국가
* 월별·대륙별 안전공지 발생 추이


각 시각화는 특정 국가가 얼마나 위험한지를 단정하기 위한 것이 아니라, 외교부 안전정보 데이터의 분포와 변화 양상을 확인하기 위한 목적으로 활용하였다.

---

## 2. 국가별 여행경보 지도

### 1. 분석 목적

국가별 여행경보 정보를 세계지도에 시각화하여 전 세계 여행경보 현황을 직관적으로 확인하고, 국가 및 지역에 따라 경보단계가 어떻게 분포하는지 탐색하고자 하였다.

여행경보 데이터는 국가 내부에서도 지역별로 서로 다른 단계가 존재할 수 있기 때문에, 단순히 국가별 한 개의 값을 사용하는 대신 각 국가의 대표 경보단계를 별도로 산정하여 지도에 표시하였다.

본 분석에서는 국가의 절대적인 위험도를 평가하기보다, 외교부 여행경보의 공간적 분포를 탐색하고 사용자에게 전체적인 안전 현황을 빠르게 전달하는 데 초점을 두었다.

---

### 2. 데이터 및 전처리

분석에는 전처리된 `alarm_clean.csv` 데이터를 사용하였다.

국가별 지도 시각화를 위해 ISO-3 코드를 기준으로 국가 위치를 연결하였다.

데이터에 ISO-3 코드가 없는 경우를 확인한 후, 별도 매핑이 필요한 국가에 대해서는 수동으로 코드를 보정하였다.

보정 이후에도 ISO-3 코드가 없는 행은 지도 시각화가 불가능하므로 제외하였다.

이를 통해 지도에서 국가 코드 누락으로 발생할 수 있는 시각화 오류를 줄였다.

---

### 3. 대표 경보단계 산정

여행경보 데이터는 하나의 국가 안에서도 여러 지역에 서로 다른 경보단계가 지정될 수 있다.

따라서 국가별 세계지도에는 모든 지역 경보를 동시에 표현하기 어렵기 때문에, 각 국가의 대표 경보단계를 별도로 산정하였다.

대표 경보단계는 해당 국가의 '대부분 지역'에 해당하는 경보를 기준으로 선정하였다.

이를 위해 `경보내용`에서 다음 표현이 포함된 데이터를 우선적으로 탐색하였다.

```python
rest_region = "제외한|전 지역|전지역|이외"
```

즉, 다음과 같은 표현이 포함된 경보를 국가의 기본 경보로 판단하였다.

* 특정 지역을 제외한 지역
* 전 지역
* 전지역
* 이외 지역

해당 조건에 해당하는 데이터는 국가별로 그룹화한 뒤 최소 경보단계를 대표값으로 사용하였다.

일부 국가는 `제외한`, `전 지역`, `이외` 등의 표현으로 대표 경보단계를 자동으로 판단하기 어려웠다.

이러한 국가들은 별도의 수동 보정값을 적용하였다.

```python
manual = {
    "EGY": 1,
    "RUS": 0,
    "COD": 0,
    "ROU": 0,
    "BOL": 0,
    "SAU": 0,
    "ETH": 0,
    "JOR": 0,
    "JPN": 0,
}
```

여기서 `0`은 실제 여행경보 0단계를 의미하는 것이 아니라, 국가 내부의 지역별 경보가 복합적으로 구성되어 하나의 대표 경보단계로 단순화하기 어려운 경우를 의미한다.

따라서 지도에서는 이를 별도의 범주인 `세부 경보 확인 필요`로 표시하였다.

---

### 4. 지도 시각화

#### 4-1. 지도 범례 및 단계별 색상

대표 경보단계는 4단계와 추가 사항 총 5개이며, 범례의 순서도 사용자가 낮은 단계에서 높은 단계로 자연스럽게 확인할 수 있도록 고정하였다.

```python
order = [
    "세부 경보 확인 필요",
    "1단계",
    "2단계",
    "3단계",
    "4단계"
]
```

여행경보 단계별 차이를 직관적으로 구분할 수 있도록 각 단계에 서로 다른 색상을 지정하였다.

이를 통해 사용자는 지도에서 국가별 경보단계 차이를 빠르게 파악할 수 있다.

#### 4-2. 세계지도 시각화

최종 지도는 Plotly의 `choropleth`를 활용하여 생성하였다.

지도에는 국가별 ISO-3 코드를 이용하여 국가 위치를 연결하고, 대표 여행경보단계에 따라 국가 영역을 구분하였다.

지도 제목을 '국가별 여행경보 단계 (대부분 지역 기준)'로 한 이유는 지도에 표시된 경보가 해당 국가의 모든 지역에 동일하게 적용되는 값이 아니라, 대부분 지역을 기준으로 선정한 대표값임을 명확히 하기 위한 것이다.

#### 4-3. 여행경보 데이터가 없는 국가 처리

여행경보 데이터가 존재하지 않는 국가는 별도의 경보 색상을 부여하지 않고 회색으로 표시하였다.

따라서 회색으로 표시된 국가는 특정 여행경보 단계가 낮다는 의미가 아니라, 본 지도에서 사용할 수 있는 여행경보 데이터가 없는 국가임을 의미한다.

---

### 5. 상세 정보 제공

#### 5-1. Hover 상세 정보

국가의 대표 경보단계만 표시할 경우 국가 내부의 세부적인 지역별 차이를 확인하기 어렵다.

이를 보완하기 위해 각 국가의 모든 지역별 여행경보 정보를 hover 영역에 함께 제공하였다.

먼저 국가별 데이터를 경보단계가 높은 순서대로 정렬하였다.

각 지역별 정보는 다음 형식으로 구성하였다.

```text
3단계 · 특정 지역 출국권고
2단계 · 일부 지역 여행자제
1단계 · 그 외 지역 여행유의
```

경보내용이 너무 길 경우 지도 hover 영역이 지나치게 커지는 것을 방지하기 위해 일정 길이 이후 텍스트를 생략하였다.

따라서 지도에서는 대표 경보단계를 빠르게 확인하면서 필요할 경우 국가 내부의 지역별 세부 경보도 함께 확인할 수 있도록 하였다.

#### 5-2. Hover 정보

사용자가 지도에서 특정 국가에 마우스를 올리면 다음 정보를 확인할 수 있도록 구성하였다.

* 국가명
* 대륙명
* 대표 여행경보단계
* 지역별 세부 여행경보

이를 통해 지도 자체는 단순하게 유지하면서도 사용자가 원하는 경우 상세한 지역별 정보를 확인할 수 있도록 하였다.

#### 5-3. 특별여행주의보 처리

여행경보 데이터 중에는 `특별여행주의보`가 언급된 경우도 존재한다.

그러나 특별여행주의보는 일반적인 여행경보 1~4단계와는 별도의 제도이기 때문에 본 지도에서 동일한 경보단계로 직접 포함하지 않았다.

대신 해당 국가의 경보내용에 특별여행주의보가 존재하면 hover 정보에 안내문을 추가하였다.

이를 통해 일반 여행경보와 특별여행주의보를 혼동하지 않도록 하였다.

---

### 6. 분석 결과

국가별 여행경보 데이터를 세계지도에 시각화함으로써 국가마다 서로 다른 여행경보가 지정되어 있으며, 하나의 국가 안에서도 지역별 경보단계가 다를 수 있다는 점을 확인할 수 있었다.

특히 일부 국가는 대부분 지역에는 낮은 단계의 경보가 적용되어 있지만 특정 지역에는 더 높은 단계의 경보가 지정되어 있었다.

따라서 국가의 가장 높은 경보단계만을 대표값으로 사용할 경우 해당 국가 전체가 높은 위험 수준으로 오해될 가능성이 있다고 판단하였다.

이를 보완하기 위해 본 지도에서는 '대부분 지역 기준'의 대표 경보단계를 사용하고, 세부 지역별 경보는 hover 정보로 별도 제공하였다.

---

### 7. 해석 시 주의사항 및 분석 한계

#### 7-1. 해석 시 주의사항

여행경보 지도를 해석할 때는 몇 가지 주의가 필요하다.

첫째, 지도에 표시된 대표 단계는 해당 국가의 모든 지역에 동일하게 적용되는 경보가 아니라 대부분 지역을 기준으로 선정한 값이다.

둘째, 특정 국가에는 대표 단계를 자동으로 산정하기 어려운 경우가 존재하여 `세부 경보 확인 필요`로 표시하였다. 해당 국가는 hover나 국가 상세 페이지를 통해 지역별 경보를 직접 확인해야 한다.

셋째, 특별여행주의보는 일반적인 여행경보 1~4단계와 다른 제도이므로 지도 대표 단계에는 포함하지 않았다.

넷째, 여행경보는 시점에 따라 변경될 수 있으므로 본 지도는 데이터 수집 시점의 여행경보 현황을 나타낸다.

다섯째, 여행경보 단계만으로 해당 국가에서 실제로 어떤 사건·사고가 발생할 가능성이 높은지는 확인할 수 없다.

따라서 여행경보 지도는 국가별 안전정보를 탐색하기 위한 첫 번째 참고자료로 활용하고, 사건사고와 최신 안전공지 등의 추가 정보도 함께 확인할 필요가 있다.

#### 7-2. 분석 한계

본 지도는 국가별 여행경보의 전체적인 공간 분포를 확인하는 데 유용하지만 몇 가지 한계가 존재한다.

우선 한 국가 내부의 여러 지역별 여행경보를 세계지도 한 화면에 모두 표현하기 어렵기 때문에 대표 경보단계를 별도로 산정해야 했다.

또한 일부 국가의 경우 경보 문구만으로 대표 단계를 자동으로 결정하기 어려워 수동 보정값을 사용하였다. 따라서 대표값 산정 과정에는 일정 부분 규칙 기반 판단이 포함되어 있다.

특별여행주의보 역시 일반 여행경보와 별도 제도이기 때문에 현재 지도에서는 직접적인 단계로 표현하지 않았다.

따라서 본 지도는 국가별 여행경보 현황을 빠르게 탐색하기 위한 시각화이며, 실제 여행 계획을 수립할 때는 국가별 상세 경보와 최신 안전정보를 추가로 확인해야 한다.

---

### 8. Streamlit 대시보드 적용

최종 Streamlit 통계 대시보드에서는 해당 지도를 첫 번째 탭인 `국가별 여행경보`에 배치하였다.

사용자는 세계지도에서 국가별 대표 여행경보를 확인하고, 특정 국가에 마우스를 올려 지역별 세부 경보를 추가로 확인할 수 있다.

지도 아래에는 다음과 같은 안내 문구를 제공하였다.

> 시각적 참고용, 자세한 정보는 챗봇이나 검색 서비스를 이용해주세요.

이를 통해 지도 결과를 절대적인 국가 위험도 지표로 해석하는 것을 방지하고, 필요한 경우 국가 상세 페이지 또는 RAG 챗봇을 통해 추가 정보를 확인하도록 구성하였다.

---

### 9. 결론

국가별 여행경보 데이터를 세계지도에 시각화하여 전 세계 경보 현황을 직관적으로 탐색할 수 있도록 하였다.

특히 국가 내부의 지역별 여행경보 차이를 고려하여 대부분 지역을 기준으로 국가별 대표 경보단계를 산정하고, 상세 지역 정보는 hover로 제공하였다.

또한 대표 단계를 단순화하기 어려운 국가는 `세부 경보 확인 필요`로 별도 구분하고, 특별여행주의보는 일반 여행경보와 분리하여 안내함으로써 정보가 과도하게 단순화되는 문제를 줄이고자 하였다.

이를 통해 사용자는 지도에서 국가별 전체적인 여행경보 현황을 확인한 뒤, 필요에 따라 지역별 경보와 사건사고, 최신 안전공지 등의 세부 정보로 탐색을 확장할 수 있도록 구성하였다.

---

## 3. 경보단계 분포 분석 (전체 국가 vs 서비스 대상 국가)

### 1. 분석 목적

전체 국가와 서비스 대상 국가의 여행경보 단계 분포를 비교하기 위해 경보단계 분포를 분석하였다.

---

### 2. 데이터 및 전처리

여행경보 데이터 `alarm.json`과 국가 매칭 데이터 `country_master.csv`를 사용하였다.

`pd.read_json`을 사용할 경우 나미비아의 ISO 코드인 `NA`가 `NaN`으로 변환되는 문제가 있어 `json.load`를 사용하였다.

컬럼명에 포함된 공백을 제거한 후 `ISO 코드`는 `iso2`, `한글 대륙명`은 `continent`로 변경하였다. `iso2`의 공백을 제거하고 `경보단계`를 숫자형으로 변환한 후 경보단계가 1~4단계에 해당하는 데이터만 사용하였다.

국가 매칭 데이터는 `keep_default_na=False`를 적용하여 나미비아의 ISO 코드 `NA`가 결측값으로 변환되지 않도록 하였으며, 첫 번째 컬럼의 BOM 처리를 위해 `utf-8-sig` 인코딩을 사용하였다.

---

### 3. 분석 방법

#### 3-1. 국가별 경보단계 정리

하나의 국가에 여러 개의 여행경보 데이터가 존재하는 경우 국가 단위로 데이터를 정리하였다.

국가별 여러 행에 대해 경보단계의 최댓값을 대표값으로 사용하고, 대륙은 첫 번째 값을 사용하였다.

```python
country = (
    alarm.groupby(["iso2", "국가명"], as_index=False)
    .agg(
        경보단계=("경보단계", "max"),
        continent=("continent", "first")
    )
)
```

#### 3-2. 서비스 대상 국가 구분

국가 매칭 데이터의 ISO 코드를 기준으로 여행경보 데이터의 국가가 서비스 대상 국가에 포함되는지 확인하였다.

여행경보 데이터에 존재하는 서비스 대상 국가의 ISO 코드를 확인한 후, 국가 매칭 데이터에는 존재하지만 여행경보 데이터에는 존재하지 않는 국가를 별도로 추출하였다.

#### 3-3. 경보단계별 국가 수 집계

전체 국가를 서비스 대상 국가와 그 외 국가로 구분한 후 경보단계별 국가 수를 집계하였다.

또한 서비스 대상 국가 중 경보단계가 2단계 이상인 국가, 전체 4단계 국가 중 서비스 대상 국가에 포함된 국가, 여행경보 데이터에 존재하지 않는 서비스 대상 국가를 각각 확인하였다.

---

### 4. 시각화 방법

경보단계별 전체 국가와 서비스 대상 국가의 분포를 스택 막대그래프로 시각화하였다.

그래프는 각 경보단계 안에서 서비스 대상 국가와 그 외 국가의 국가 수를 함께 확인할 수 있도록 스택 형태로 구성하였다.

경보단계는 다음과 같이 구분하였다.

* 1단계: 여행유의
* 2단계: 여행자제
* 3단계: 출국권고
* 4단계: 여행금지

---

### 5. 분석 결과

경보 미지정 국가 수, 전체 4단계 국가 중 서비스 대상 국가에 포함된 국가 수, 서비스 대상 국가 중 2단계 이상인 국가 수를 핵심 수치로 제공하였다.

서비스 대상 국가 중 경보단계가 2단계 이상인 국가가 존재하는 경우 해당 국가의 국가명, 대륙, 경보단계를 표로 제공하였다.

---

### 6. 해석 시 주의사항 및 분석 한계

여행경보가 아예 없는 서비스 대상 국가는 경보 미지정 국가로 별도로 처리하였다.

또한 서비스 대상 국가 중 경보단계가 2단계 이상인 국가는 방문 전 확인이 필요한 국가로 별도로 확인할 수 있도록 구성하였다.

---

### 7. Streamlit 대시보드 적용

Streamlit에서는 `추천 국가 안전도 한눈에 보기` 항목으로 제공하였으며, 사용자는 `여행지 추천이 가능한 국가 보기`를 통해 서비스 대상 국가 목록을 확인할 수 있다. 경보단계별 스택 막대그래프를 통해 서비스 대상 국가와 그 외 국가의 분포를 확인할 수 있도록 하였다.

또한 다음 세 가지 핵심 수치를 제공하였다.

* 경보 미지정 국가 수
* 4단계 국가 중 서비스 대상 국가에 포함된 국가 수
* 서비스 대상 국가 중 2단계 이상인 국가 수

서비스 대상 국가 중 경보단계가 2단계 이상인 국가가 존재하는 경우 해당 국가의 국가명, 대륙, 경보단계를 표로 제공하였다.

---

## 4. 연도별 국가 안전정보 TOP 10

### 1. 분석 목적

연도별 안전정보 등록 현황을 확인하고, 각 연도에서 안전정보가 많이 등록된 국가 TOP10을 비교하였다.

이를 통해 전체 안전정보의 규모가 연도에 따라 어떻게 달라지는지 확인하고, 특정 연도에 안전정보가 상대적으로 집중되어 등록된 국가가 있는지 탐색하였다.

단, 안전정보 등록 건수는 실제 국가의 위험도를 직접적으로 나타내는 지표가 아니므로 국가별 위험도를 평가하기보다는 데이터의 등록 현황과 분포를 파악하는 데 초점을 두었다.

---

### 2. 데이터 및 전처리

분석에는 국가별·연도별 안전정보 등록 건수를 집계한 `safety_stats.csv`를 활용하였다.

데이터의 연도 범위를 확인한 후, 연도별 비교 분석에서는 2011년부터 2024년까지의 데이터를 사용하였다.

---

### 3. 분석 방법

#### 3-1. 2024년 국가별 안전정보 TOP10

먼저 하나의 연도 안에서 국가별 안전정보 등록 건수에 어떤 차이가 나타나는지 확인하기 위해 2024년 데이터를 기준으로 등록 건수가 많은 상위 10개 국가를 추출하였다.

#### 3-2. 2011~2024년 연도별 국가 안전정보 TOP10

특정 연도뿐만 아니라 장기간에 걸쳐 안전정보 등록 현황이 어떻게 달라졌는지 확인하기 위해 2011년부터 2024년까지 각 연도의 국가별 안전정보 등록 건수 TOP10을 비교하였다.

각 연도에 대해 다음 값을 함께 계산하였다.

* 해당 연도의 전체 안전정보 등록 건수

* 해당 연도 TOP10 국가의 안전정보 등록 건수 합계

* 안전정보 등록 건수가 많은 상위 10개 국가

---

### 4. 시각화 방법

국가별 등록 건수를 내림차순으로 정렬한 뒤 상위 10개 국가를 선정하고, 국가 간 차이를 쉽게 비교할 수 있도록 가로 막대그래프로 시각화하였다.

연도별 TOP10을 각각 가로 막대그래프로 시각화하고 하나의 그래프에 배치하여 연도별 차이를 비교하였다.

---

### 5. 분석 결과

#### 5-1. 2024년 분석 결과

2024년 데이터를 기준으로 국가별 안전정보 등록 건수를 비교한 결과, 상위 10개 국가 사이에서도 등록 건수에 차이가 나타나는 것을 확인하였다.

다만 2024년 데이터는 7월 23일까지 수록된 자료로 완전한 연간 데이터가 아니다.

따라서 2024년의 전체 등록 건수를 다른 연도의 연간 등록 건수와 직접 비교하기보다는, 2024년 데이터 내에서 어떤 국가의 안전정보가 상대적으로 많이 등록되었는지를 확인하는 용도로 해석하였다.

#### 5-2. 2011~2024년 분석 결과

분석 결과, 전체 안전정보 등록 건수는 연도에 따라 큰 편차를 보였다.

특정 연도에는 상대적으로 많은 안전정보가 등록된 반면, 2021년 이후에는 이전 시기에 비해 전체 등록 건수가 크게 감소하는 모습을 확인할 수 있었다.

또한 연도별 TOP10 국가의 구성이 동일하게 유지되지 않고 시기에 따라 달라지는 것을 확인하였다. 즉, 특정 국가에 지속적으로 안전정보가 집중되기보다는 연도에 따라 안전정보가 상대적으로 많이 등록된 국가가 달라지는 양상이 나타났다.

이는 안전정보 데이터가 시간에 따라 동일한 형태로 분포하는 것이 아니라 특정 시기와 국가에 따라 등록량이 달라질 수 있음을 보여준다.

---

### 6. 해석 시 주의사항 및 분석 한계

#### 6-1. 연도별 등록 건수 해석

연도별 등록 건수의 차이가 크게 나타났지만 현재 데이터만으로 그 원인을 특정하기는 어렵다.

등록 건수에는 다음과 같은 요인이 영향을 줄 가능성이 있다.

* 특정 국가에서 발생한 사건·사고

* 자연재해 및 감염병 등의 발생

* 정치·사회적 상황 변화

* 안전정보 등록 및 관리 정책의 변화

* 데이터 수집 범위의 차이

따라서 **특정 연도의 등록 건수가 증가했다고 해서 해당 시기에 전체 국가의 위험도가 증가했다고 판단하거나, 반대로 등록 건수가 감소했다고 해서 여행 안전 수준이 개선되었다고 해석할 수는 없다.**

본 분석에서 등록 건수는 실제 위험 수준이 아닌 **안전정보 데이터의 등록 및 분포 현황을 나타내는 탐색적 지표**로 활용하였다.

#### 6-2. 분석 한계

본 분석에는 몇 가지 한계가 존재한다.

첫째, 안전정보 등록 건수는 실제 국가의 위험 수준을 직접적으로 나타내는 지표가 아니다. 등록 건수는 실제 사건뿐만 아니라 정보 작성 및 관리 방식의 영향을 받을 수 있다.

둘째, 연도별 데이터의 수집 범위와 조건이 동일하다고 단정할 수 없기 때문에 등록 건수의 증감을 실제 안전상황의 변화로 해석하는 데 주의가 필요하다.

셋째, 2024년 데이터는 7월 23일까지 수록되어 있어 완전한 연간 데이터가 아니다. 따라서 2024년의 절대적인 등록 건수를 이전 연도의 전체 연간 데이터와 직접 비교하는 것은 적절하지 않다.

---

### 7. Streamlit 대시보드 적용

EDA에서 확인한 연도별 국가 TOP10 분석을 최종 Streamlit 통계 대시보드에도 적용하였다.

노트북 EDA에서는 2011~2024년의 모든 연도별 TOP10 그래프를 한 번에 비교하였지만, Streamlit에서는 사용자가 원하는 연도를 직접 선택할 수 있도록 연도 슬라이더를 적용하였다.

선택된 연도에 대해 다음 정보를 제공한다.

* 국가별 안전정보 등록 건수 TOP10

* 해당 연도의 전체 안전정보 등록 건수

* TOP10 국가의 등록 건수 합계

이를 통해 전체 연도를 한 번에 보여주는 복잡한 시각화 대신 사용자가 원하는 연도를 선택하여 국가별 등록 현황을 확인할 수 있도록 구성하였다.

---

### 8. 결론

2011년부터 2024년까지 국가별 안전정보 등록 건수를 분석한 결과, 연도별 전체 등록 건수에 상당한 차이가 있으며 안전정보가 많이 등록된 국가의 구성 역시 연도에 따라 달라지는 것을 확인하였다.

특히 2021년 이후에는 이전 시기보다 전체 등록 건수가 감소하는 경향이 나타났지만, 현재 데이터만으로 이러한 변화의 원인을 특정하기는 어렵다.

따라서 본 분석은 국가의 실제 위험도를 평가하기 위한 분석이 아니라, 안전정보 데이터가 연도와 국가에 따라 어떻게 분포되어 있는지를 파악하기 위한 EDA로 활용하였다.

분석 결과는 최종 Streamlit 통계 대시보드의 연도별 국가 안전정보 TOP10 기능으로 연결하여 사용자가 연도별 등록 현황을 직접 탐색할 수 있도록 하였다.

---

## 5. 대륙별 월간 안전공지 추이

### 1. 분석 목적

안전공지가 시간에 따라 어떻게 변화하는지 확인하고, 대륙별로 안전공지 발생 패턴에 차이가 있는지 분석하였다.

단순히 전체 안전공지 건수만 비교하는 것이 아니라, 작성일 정보를 활용해 연도와 월 단위로 데이터를 세분화하고 대륙별 추이를 함께 비교할 수 있도록 구성하였다.

---

### 2. 데이터 및 전처리

분석에는 `safety_notice_processed.csv`를 사용하였다.

먼저 `안전공지_작성일` 컬럼을 날짜형으로 변환하였다.

날짜 변환 과정에서 정상적으로 변환되지 않는 값은 `NaT`로 처리하였다.

이후 날짜 정보를 기준으로 연도와 월 파생변수를 생성하였다.

이를 통해 기존의 작성일 데이터를 월 단위 시계열 분석이 가능한 형태로 변환하였다.

---

### 3. 분석 방법

#### 3-1. 집계 방법

안전공지의 월별·대륙별 분포를 확인하기 위해 다음 세 변수를 기준으로 그룹화하였다.

* 연도

* 월

* 대륙명

```python
monthly_continent = (
    df.dropna(subset=["대륙명", "연도", "월"])
      .groupby(["연도", "월", "대륙명"])
      .size()
      .reset_index(name="공지건수")
)
```

분석에 필요한 `대륙명`, `연도`, `월` 중 하나라도 결측된 데이터는 집계에서 제외하였다.

그룹별 행의 개수를 계산하여 `공지건수`라는 새로운 변수로 생성하였다.

이를 통해 특정 연도와 월에 각 대륙에서 몇 건의 안전공지가 등록되었는지 비교할 수 있도록 하였다.

#### 3-2. 인터랙티브 필터 구성

EDA 단계에서는 `ipywidgets`를 활용하여 연도와 대륙을 선택할 수 있도록 구성하였다.

#### 3-3. 연도 필터

연도는 다음 세 가지 조건을 선택할 수 있도록 하였다.

* 전체

* 2025년

* 2026년

```python
year_dropdown = widgets.Dropdown(
    options=["전체", 2025, 2026],
    value="전체",
    description="연도:"
)
```

전체를 선택하면 모든 연도의 데이터를 확인할 수 있고, 특정 연도를 선택하면 해당 연도의 월별 변화만 확인할 수 있다.

#### 3-4. 대륙 필터

대륙은 데이터에 존재하는 대륙명을 자동으로 불러오도록 구성하였다.

```python
continent_dropdown = widgets.Dropdown(
    options=["전체"] + sorted(
        monthly_continent["대륙명"].dropna().unique().tolist()
    ),
    value="전체",
    description="대륙:"
)
```

전체 대륙을 동시에 비교할 수도 있고, 하나의 대륙을 선택하여 해당 지역의 월별 변화만 집중적으로 확인할 수도 있다.

#### 3-5. 필터링 방식

사용자가 선택한 조건에 따라 집계 데이터를 필터링하였다.

```python
if year != "전체":
    chart_df = chart_df[
        chart_df["연도"] == year
    ]

if continent != "전체":
    chart_df = chart_df[
        chart_df["대륙명"] == continent
    ]
```

이를 통해 다음과 같은 분석이 가능하다.

* 전체 기간의 대륙별 안전공지 비교

* 특정 연도의 대륙별 안전공지 비교

* 특정 대륙의 전체 기간 월별 변화 확인

* 특정 연도와 특정 대륙을 동시에 선택한 세부 분석

---

### 4. 시각화 방법

월별 변화를 확인하기 위해 선 그래프를 사용하였다.

그래프 구성은 다음과 같다.

* X축: 연월

* Y축: 안전공지 건수

* 색상 구분: 대륙명

* 마커: 각 월별 관측값

```python
fig = px.line(
    chart_df,
    x="연월표시",
    y="공지건수",
    color="대륙명",
    markers=True,
    title="월별 대륙별 안전공지 추이"
)
```

각 월의 값을 명확하게 확인할 수 있도록 선 그래프에 마커를 함께 표시하였다.

또한 동일한 월에서 여러 대륙의 값을 쉽게 비교할 수 있도록 `hovermode="x unified"`를 적용하였다.

---

### 5. 해석 시 주의사항 및 분석 한계

안전공지 건수가 증가했다고 해서 해당 대륙의 실제 위험 수준이 같은 비율로 증가했다고 해석할 수는 없다.

또한 대륙마다 포함된 국가 수와 여행객 규모가 다르기 때문에 단순한 공지 건수만으로 대륙 간 위험도를 직접 비교하기에는 한계가 있다.

따라서 본 분석에서는 월별·대륙별 안전공지 건수를 실제 위험도 지표가 아니라, 안전정보가 시간과 지역에 따라 어떻게 분포되어 있는지를 확인하기 위한 탐색적 지표로 활용하였다.

---

### 6. Streamlit 대시보드 적용

EDA에서 확인한 분석 구조는 최종 Streamlit 통계 대시보드에도 동일하게 적용하였다.

Streamlit에서는 사용자가 연도와 대륙을 선택하면 해당 조건에 맞는 데이터를 즉시 필터링하여 그래프가 변경되도록 구현하였다.

이를 통해 정적인 그래프 하나를 제공하는 대신 사용자가 직접 원하는 연도와 지역을 선택하여 안전공지의 변화 양상을 탐색할 수 있도록 하였다.

---

## 6. 통계 대시보드 종합 해석

네 가지 시각화를 통해 해외여행 안전정보를 서로 다른 관점에서 탐색할 수 있도록 구성하였다.

국가별 여행경보 지도는 현재 국가별 경보 수준을 공간적으로 비교할 수 있으며, 연도별 국가 안전정보 TOP10 분석은 특정 시기에 안전 관련 정보가 집중된 국가를 파악하는 데 활용할 수 있다.

대륙별 월간 안전공지 추이는 시간에 따른 안전공지 발생 변화와 지역별 차이를 확인할 수 있도록 하였다.

경보단계 분포 분석은 전체 국가와 서비스 대상 국가를 구분하여 경보단계별 국가 수를 비교하고, 서비스 대상 국가 중 경보 미지정 국가와 2단계 이상 국가, 전체 4단계 국가 중 서비스 대상 국가에 포함된 국가를 함께 확인할 수 있도록 하였다.

각 데이터는 서로 다른 특성을 가지고 있기 때문에 하나의 지표만으로 국가의 안전 수준을 판단하기보다는 여행경보, 사건사고, 안전공지 정보를 함께 확인하는 것이 중요하다.

따라서 본 프로젝트에서는 통계 대시보드를 전체적인 안전정보 현황을 탐색하기 위한 기능으로 활용하고, 개별 국가에 대한 세부 정보는 국가 상세 페이지와 RAG 챗봇을 통해 확인할 수 있도록 구성하였다.